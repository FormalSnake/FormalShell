#!/usr/bin/env bash
# Kept as the name CLAUDE.md and older notes point at; dev/smoke.sh is the
# Hyprland rig now (M41), with the --console leg this script used to own.
exec "$(dirname "$0")/smoke.sh" "$@"
